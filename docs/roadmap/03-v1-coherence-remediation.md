# V1 coherence remediation

This is correction work only: preserve the established north star, resolved V1 scope, light-paper skin stance, and existing release decisions. Do not add components, skins, effects, visual redesign, baseline or snapshot updates, dependencies, version bumps, downstream pin changes, release publishing/tagging, or fabricated human/device evidence. Every change must retain the existing gates and add only targeted regression coverage where its contract needs protection.

- [x] **motion-contract-consistency** (archived as `2026-08-25-motion-contract-consistency`) — Reconcile the shipped motion values and atmosphere timing with the stepped-clock and immediate-interaction contract: remove or neutralise smooth UI transition/easing affordances and ensure periodic motion uses the established stepped behaviour, without inventing motion or changing the visual baseline; add a focused contract regression and retain reduced-motion coverage.
- [x] **skin-aware-effects-ink** (archived as `2026-08-25-skin-aware-effects-ink`) — Replace every remaining cream ink literal in shipped grid, wind, rain, splash, mote, and snow effects with the existing ink role so complete skins reskin effects as well as surfaces; add a focused all-skins effect-ink regression and retain token, build, validation, and existing tests.
- [x] **utility-mark-canonical-examples** (archived as `2026-08-25-utility-mark-canonical-examples`) — Correct the canonical field and theme labels (and any equivalent prose examples) to lowercase Archivo treatment, reserving the existing mono-uppercase utility mark for data only; add targeted markup/style regression coverage without adding components or changing the resolved type language.
- [ ] **safe-whole-app-island-integration** — Repair the consumer guide’s end-to-end paths: make whole-app adoption explicitly mount the shipped component/effect classes, and make island adoption use the scoped bundles and `values/shadcn/adapter.scoped.css` under the correct `.ontwerp` root, including the existing boundary and leakage safeguards; verify documentation paths against the assembled bundle without redesign or new integration machinery.
- [ ] **exact-readiness-evidence** — Reconcile `docs/releases/v1.0.0-readiness.md` with the actual current gate output (including the exact strict-spec count), rerun build, validation, tests, strict specs, and reproducibility evidence after the corrections, record only observed results and leave human/device checks explicitly human-owned; do not publish, tag, push, bump a version, or advance any downstream pin.
