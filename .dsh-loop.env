CHECK_CMD="npm run check"
BROWSER_CMD="npm test"

PLAN_MODEL=luna-xhigh
CODE_MODEL=ox-alpha
REVIEW_MODEL=ox-alpha
FINISH_MODEL=ox-alpha
FINISH_FALLBACK_MODEL=luna

VERIFY_NOTE="
Read V1_WORKER_HANDOFF.md after AGENTS.md and follow it exactly. The worker owns only C6, C5, C7,
and C11 in that order. Never create or draft C8, C9, C10, C13, or C14. Never publish a release or
push a release branch/tag. Never regenerate or edit the accepted zoo baseline to repair parity.
Headless showcase screenshots are prohibited. V1_WORKER_HANDOFF.md explicitly records C6 task 5.10
as owner-verified in the live zoo; leave that checkbox unchecked and treat the handoff as the owner
confirmation required to archive C6 once every automated task and gate passes.
"
