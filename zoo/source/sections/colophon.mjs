import { leafMark } from '../effects/phyllotaxis.mjs';

export function renderColophon() {
  return `<footer class="colophon">${leafMark()}<span>een ontwerpsysteem voor floris kappens projecten — en voor wie er verder gebruik van wil maken.</span></footer>`;
}
export const implementsRecipes = [];
