# ROADMAP-FEEDBACK

C8's done condition held up, but the roadmap does not mention that the zoo has a
class-level parity gate (every class emitted in the showcase must be targeted by some CSS
rule), so "add a worked example" implicitly requires every variant class to carry its own
styling — worth knowing before planning zoo additions, since it shapes markup/CSS design
(not just whether tests pass).
